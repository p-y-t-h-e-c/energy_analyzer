load_from:
  # Each entry here corresponds to a service in the docker-compose file that exposes user code.
  - grpc_server:
      host: energy_analyzer_docker_code_base
      port: 4000
      location_name: "energy_analyzer"
